import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.andres.heroesvillains',
  appName: 'Heroes Villains',
  webDir: 'www'
};

export default config;
